# =============================================================================
# APiGen Studio - Development Dockerfile
# =============================================================================
# Hot reload development server with Vite
#
# Note: Runs as root for development to avoid permission issues with volume mounts.
# Production Dockerfile uses non-root user for security.
# =============================================================================

FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package.json package-lock.json ./

# Install dependencies
RUN npm ci

# Create .vite cache directory with proper permissions (for volume mounts)
RUN mkdir -p /app/node_modules/.vite && chmod -R 777 /app/node_modules/.vite

# Copy source code
COPY src/ ./src/
COPY public/ ./public/
COPY index.html vite.config.ts tsconfig.json tsconfig.node.json postcss.config.cjs ./

# Expose Vite dev server port
EXPOSE 5173

# Start development server with host binding for Docker
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
